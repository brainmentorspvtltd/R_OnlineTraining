# Vectors -> 1D Collection of similar elements
# List -> Collection of any type of data
# Matrix -> 2D type of data like (row x column)
# DataFrame -> Contains data in row column but tabular format
# Factors -> Collection of categorical type of data
# Array -> n-dimensional collection of similar type of data

vec <- c(1,2,3,5,6,7,9,15)
vec <- 1:20
vec <- seq(from=1, to=30, by=2)

#Random Data
vec <- sample(20)

# generate 15 random numbers from 20 to 50
vec <- sample(20:50, size = 15)

